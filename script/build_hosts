#!/usr/bin/python3

import yaml
import argparse
import os
import datetime

parser = argparse.ArgumentParser(description="script to build hosts.yml")
parser.add_argument(
    "-w", action="store_true", dest="wf", help="enable world finals mode"
)
args = parser.parse_args()

judges = {}
humanjudges = {}
kattis_autojudges = {}
pc2aj = {}
domjudge_judges = {}
servers = {}
kattis_ccsadmin = {}
domjudge_ccsadmin = {}
pc2_ccsadmin = {}
contestants = {}
admins = "    troy-mgmt:\n    keith-mgmt:\n"
if args.wf:
    name = "wf2025"
    detail = "2025Baku"
    timezone = "Asia/Baku"
    max_team = 146
    max_human_judge = 15
    max_pc2_autojudge = 12
    max_pc2_ccsadmin = 4
    max_kattis_ccsadmin = 0
    max_kattis_autojudge = 0
    max_domjudge_autojudge = 10
    max_domjudge_ccsadmin = 4
    max_coachview = 8
    papersize = "A4"
    enable_vlc = True
    ccs = "pc2"
    need_reverse_proxy = False
    local_kattis_server = False
    backup_kattis_server = False
    local_pc2_server = True
    backup_pc2_server = True
    local_domjudge_server = True
    backup_domjudge_server = True
else:
    name = "nac2025"
    detail = "2025Florida"
    timezone = "America/New_York"
    max_team = 55
    max_human_judge = 6
    max_pc2_autojudge = 6
    max_kattis_autojudge = 0
    max_pc2_ccsadmin = 6
    max_domjudge_autojudge = 6
    max_domjudge_ccsadmin = 1
    max_kattis_ccsadmin = 0
    max_coachview = 8
    papersize = "letter"
    enable_vlc = False
    ccs = "pc2"
    need_reverse_proxy = True
    local_kattis_server = False
    backup_kattis_server = False
    local_pc2_server = False
    backup_pc2_server = False
    local_domjudge_server = False
    backup_domjudge_server = False


def print_host(num, hostname, ip, list_fh, group, base_ip=0):
    group[f"{hostname}{num}"] = {"ansible_host": f"{ip}.{base_ip+num}"}
    print(f"{hostname}{num}", file=list_fh)


def print_host_wf(base, num, hostname, ip, list_fh, group, base_ip=0):
    group[f"{hostname}{num}"] = {"ansible_host": f"{ip}.{base_ip+num-base}"}
    print(f"{hostname}{num}", file=list_fh)


def print_server(hostname, ip, group=[]):
    group[f"{hostname}:\n"] = ""


def print_children(max_label, item, document):
    if max_label:
        document += f"""
    {item}:"""
    return document


if __name__ == "__main__":
    document = f"""---
all:
  vars:
    ccs: {ccs}
    timezone: {timezone}
    root_password: null
    disable_sources_list_changes: false
    enable_reverseproxy: {str(need_reverse_proxy).lower()}
    contest: {name}
    contest_detail: {detail}
    papersize: {papersize}
    enable_vlc: {str(enable_vlc).lower()}"""

    # make a backup of ../hosts.yaml with the it's modified date, if it exists
    if os.path.isfile("../hosts.yml"):
        FilePath = "../hosts.yml"
        modifiedTime = os.path.getmtime(FilePath)
        timeStamp = datetime.datetime.fromtimestamp(modifiedTime).strftime(
            "%b-%d-%y-%H:%M:%S"
        )
        os.rename(FilePath, FilePath + "_" + timeStamp)
    with open("../hosts.yml", "wt") as fh:
        print(document, file=fh)
        document2 = ""
        document = ""
        with open("../list.judge", "wt") as list_fh:
            for i in range(max_human_judge):
                print_host(i + 1, "judge", "10.2.2", list_fh, humanjudges)
        with open("../list.autojudges", "wt") as list_fh:
            for i in range(max_kattis_autojudge):
                print_host(i + 1, "autojudge", "10.2.2", list_fh, kattis_autojudges, 31)
        with open("../list.pc2judges", "wt") as list_fh:
            for i in range(max_pc2_autojudge):
                print_host(i + 1, "pc2-aj", "10.2.2", list_fh, pc2aj, 127)
        with open("../list.domjudges", "wt") as list_fh:
            for i in range(max_domjudge_autojudge):
                print_host(
                    i + 1, "domjudge-judgehost", "10.2.2", list_fh, domjudge_judges, 191
                )
        document3 = ""
        document2 += f"""
autojudges:
  children:"""
        if max_domjudge_autojudge > 0:
            document2 += print_children(
                max_label=max_domjudge_autojudge,
                item="domjudge_judgehost",
                document=document3,
            )
        if max_kattis_autojudge > 0:
            document2 += print_children(
                max_label=max_kattis_autojudge,
                item="kattis_autojudge",
                document=document3,
            )
        if max_pc2_autojudge > 0:
            document2 += print_children(
                max_label=max_pc2_autojudge, item="pc2_aj", document=document3
            )
        document2 += """

judges:
  children:"""
        document2 += print_children(
            max_label=max_human_judge, item="human_judge", document=document3
        )
        if max_kattis_autojudge > 0:
            document2 += print_children(
                max_label=1,
                item="kattis-autojudges",
                document=document3,
            )

        document = ""
        if max_human_judge:
            humanjudges = f'\n    "judge[1:{max_human_judge}]":'
            document += f"""
human_judges:
  hosts:{humanjudges}
"""
        if max_domjudge_autojudge:
            domjudge_judges = f'\n    "domjudge-judgehost[1:{max_domjudge_autojudge}]":'
            document += f"""
domjudge_judgehost:
  hosts:{domjudge_judges}
"""
        if max_kattis_autojudge:
            kattis_judges = f'\n    "kattis-autojudge[1:{max_kattis_autojudge}]":'
            document += f"""
kattis_autojudge:
  hosts:{kattis_judges}
"""
        if max_pc2_autojudge:
            pc2aj = f'\n    "pc2-aj[1:{max_pc2_autojudge}]":'
            document += f"""
pc2_aj:
  hosts:{pc2aj}
"""

        print(document, file=fh, end="")
        document = ""
        print(document2, file=fh)
        with open("../list.domjudges_ccsadmin", "wt") as list_fh:
            for i in range(max_domjudge_ccsadmin):
                print_host(
                    i + 1,
                    "domjudge-ccsadmin",
                    "10.3.3",
                    list_fh,
                    domjudge_ccsadmin,
                    223,
                )
        with open("../list.ccsadmin", "wt") as list_fh:
            for i in range(max_kattis_ccsadmin):
                print_host(i + 1, "kattis-ccsadmin", "10.3.3", list_fh, ccsadmin, 231)
        with open("../list.pc2_ccsadmin", "wt") as list_fh:
            for i in range(max_pc2_ccsadmin):
                print_host(i + 1, "pc2-ccsadmin", "10.3.3", list_fh, pc2_ccsadmin, 239)
        if need_reverse_proxy:
            print_server("reverseproxy", "10.3.3.205", servers)
        # print_server("media", "10.3.3.206", servers);
        print_server("cds", "10.3.3.207", servers)
        print_server("scoreboard", "10.3.3.208", servers)
        print_server("packages", "10.3.3.209", servers)
        print_server("backup", "10.3.3.210", servers)
        print_server("printsrv", "10.3.3.211", servers)
        # this is windows box
        # print_server("balloonmanager", "10.4.4.149", servers)
        if local_kattis_server:
            print_server("kattis", "10.3.3.212", servers)
            print_server("kattisbackup", "10.3.3.213", servers)
        if local_pc2_server:
            print_server("pc2", "10.3.3.214", servers)
            if backup_pc2_server:
                print_server("pc2backup", "10.3.3.222", servers)
        if local_domjudge_server:
            print_server("domjudge-1", "10.3.3.216", servers)
            if backup_domjudge_server:
                print_server("domjudge-2", "10.3.3.217", servers)

        document2 = ""
        if max_domjudge_ccsadmin > 0:
            document2 += f"""
domjudge_ccsadmin:
  hosts:
    "domjudge-ccsadmin[1:{max_domjudge_ccsadmin}]":"""
        if max_kattis_ccsadmin > 0:
            document2 += f"""
kattis_ccsadmin:
  hosts:
    "kattis-ccsadmin[1:{max_kattis_ccsadmin}]":"""
        if max_pc2_ccsadmin > 0:
            document2 += f"""
pc2_ccsadmin:
  hosts:
    "pc2-ccsadmin[1:{max_pc2_ccsadmin}]":"""
        document2 += f"""
ccsadmin:
  children:"""
        if max_domjudge_ccsadmin > 0:
            document2 += f"\n    domjudge_ccsadmin:"
        if max_kattis_ccsadmin > 0:
            document2 += f"\n    kattis_ccsadmin:"
        if max_pc2_ccsadmin > 0:
            document2 += f"\n    pc2_ccsadmin:"
        document3 = ""
        #        document2 += print_children(max_label=1, item="servers", document=document3)
        servers = f"""    cds:
    packages:
    backup:
    printsrv:"""
        if local_domjudge_server:
            servers += "\n    domserver-1:"
            if backup_domjudge_server:
                servers += "\n    domserver-2:"
        if local_kattis_server:
            servers += "\n    kattis:"
            if backup_kattis_server:
                servers += "\n    kattisbackup:"
        if local_pc2_server:
            servers += "\n    pc2:"
            if backup_pc2_server:
                servers += "\n    pc2backup:"
        document = f"""
admin:
  hosts:
{admins}

servers:
  hosts:
{servers}
"""
        document += f"""
windows:
  children:
    presentation_admins:
      hosts:
        presadmin1:
          ansible_host: 10.3.3.190
    presentation_clients:
      hosts:
        presclient1:
          ansible_host: 10.3.3.1
          presclient_name: "preclient1"
        presclient2:
          ansible_host: 10.3.3.2
          presclient_name: "preclient2"
        presclient3:
          ansible_host: 10.3.3.3
          presclient_name: "preclient3"
        presclient4:
          ansible_host: 10.3.3.4
          presclient_name: "preclient4"
        presclient5:
          ansible_host: 10.3.3.5
          presclient_name: "preclient5"
        presclient6:
          ansible_host: 10.3.3.6
          presclient_name: "preclient6"
        presclient7:
          ansible_host: 10.3.3.7
          presclient_name: "preclient7"

        green-presclient0:
          ansible_host: 172.29.1.60
          presclient_name: green-presclient0
        green-presclient1:
          ansible_host: 172.29.1.61
          presclient_name: green-presclient1
        green-presclient2:
          ansible_host: 172.29.1.62
          presclient_name: green-presclient2
        green-presclient3:
          ansible_host: 172.29.1.63
          presclient_name: green-presclient3
        green-presclient4:
          ansible_host: 172.29.1.64
          presclient_name: green-presclient4
        green-presclient5:
          ansible_host: 172.29.1.65
          presclient_name: green-presclient5
        green-presclient6:
          ansible_host: 172.29.1.66
          presclient_name: green-presclient6
        green-presclient7:
          ansible_host: 172.29.1.67
          presclient_name: green-presclient7
        green-presclient8:
          ansible_host: 172.29.1.68
          presclient_name: green-presclient8
        green-presclient9:
          ansible_host: 172.29.1.69
          presclient_name: green-presclient9
    win_balloonprinters:
      hosts:
        balloonmanager:
          ansible_host: 10.4.4.149
    win_coachviews:
      hosts:
"""
        if max_coachview > 0:
            document += f'        "coachview[1:{max_coachview}]":'
        print(document, file=fh)
        print(document2, file=fh)
        document = f"""
grafana:
  hosts:
    backup:
ntpservers:
  hosts:
    printsrv:
    backup:
    packages:
"""
        print(document, file=fh)
        with open("../list.team", "wt") as list_fh:
            for i in range(max_team):
                print_host(i + 1, "team", "10.1.1", list_fh, contestants)
            contestants = f'"team[1:{max_team}]":'
            document = f"""
contestants:
  children:
    floor:
"""
            document2 = f"""
floor:
  hosts:
    {contestants}
"""
            print(document2, file=fh, end="")
            print(document, file=fh, end="")
